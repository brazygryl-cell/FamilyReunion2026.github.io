[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 👇 IMPORTANT — this forces the service key to be packaged
[[functions]]
  path = "netlify/functions"
  included_files = ["netlify/functions/service-account.json"]
