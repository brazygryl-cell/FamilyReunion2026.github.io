[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/service-account.json"]

[build.environment]
SECRETS_SCAN_DISABLED = "true"
